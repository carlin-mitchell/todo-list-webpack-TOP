import Element from "../Element";

// DYNAMIC VARIABLES

// LINK STRINGS

// ANIMATION METHODS

// COMPONENT
const Main = () => {
  const parentContainer = Element("main", { id: "main" });
  return parentContainer;
};

export default Main;
