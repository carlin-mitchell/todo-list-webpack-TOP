import Element from "../Element";

// DYNAMIC VARIABLES

// LINK STRINGS

// ANIMATION METHODS

// COMPONENT
const Footer = () => {
  const parent = Element("div", {});
  return parent;
};

export default Element;
